<section class="list">
    <ul>
    {{#each career in model}}
        <li>
            <a {{action "pick1" career}}>
                {{#if career.loaded}}&#8862;{{/if}}
                {{render_career career}}
            </a>
        </li>
    {{/each}}
    </ul>
</section>

